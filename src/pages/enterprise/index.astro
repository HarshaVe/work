---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const portfolioEntries = await getCollection('portfolio');
const filteredEntries = portfolioEntries
  .filter(entry => entry.data.featured.enterprise)
  .sort((a, b) => a.data.order - b.data.order);

const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Enterprise B2B' }
];

const actionLink = { label: 'view full resume', href: '/resume' };
---

<Layout title="Enterprise B2B" breadcrumbs={breadcrumbs} actionLink={actionLink}>
  <h1>Enterprise B2B</h1>
  
  <p class="intro">Enterprise B2B design means understanding that your users are professionals with complex workflows, competing priorities, and little patience for friction. I've led teams that simplify the complex — turning industrial systems into intuitive experiences that drive adoption and business results.</p>

  <div class="highlight-box">
    <h3>Key Outcomes</h3>
    <ul>
      <li><strong>CARFAX:</strong> Redefined brand and product experience, establishing trust as the market differentiator and achieving 7% conversion lift</li>
      <li><strong>Bosch Power Tools:</strong> Led consumer research that shaped B2C eCommerce strategy and uncovered new revenue streams through asset management</li>
      <li><strong>Siemens:</strong> Delivered measurable impact through product scorecards — 26% engagement lift and 17% conversion improvement</li>
    </ul>
  </div>

  <h2>Related Work</h2>
  
  <ul class="portfolio-list">
    {filteredEntries.map((entry) => (
      <li class="portfolio-list-item">
        {entry.data.heroImage && (
          <a href={`/portfolio/${entry.slug}?via=enterprise`} class="portfolio-thumbnail">
            <img src={entry.data.heroImage} alt={entry.data.title} />
          </a>
        )}
        <div class="portfolio-list-content">
          <a href={`/portfolio/${entry.slug}?via=enterprise`} class="portfolio-title">{entry.data.title}</a>
          <span class="portfolio-company"> — {entry.data.company}</span>
          <p>{entry.data.description}</p>
        </div>
      </li>
    ))}
  </ul>
  
  <p><a href="/portfolio">View entire portfolio</a></p>
</Layout>
