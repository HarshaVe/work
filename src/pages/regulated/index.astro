---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const portfolioEntries = await getCollection('portfolio');
const filteredEntries = portfolioEntries
  .filter(entry => entry.data.featured.regulated)
  .sort((a, b) => a.data.order - b.data.order);

const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Regulated Industries' }
];

const actionLink = { label: 'view full resume', href: '/resume' };
---

<Layout title="Regulated Industries" breadcrumbs={breadcrumbs}>
  <h1>Regulated Industries</h1>
  
  <p class="intro">I've spent significant parts of my career designing consumer experiences within highly regulated environments — healthcare, government, automotive safety, and financial services. These aren't just design challenges; they're exercises in building trust while navigating compliance.</p>

  <div class="highlight-box">
    <h3>Key Outcomes</h3>
    <ul>
      <li><strong>CDC:</strong> Designed and developed the first responsive federal campaign site under HIPAA and Section 508 accessibility requirements, increasing HIV testing conversions by 70%</li>
      <li><strong>GM & Siemens:</strong> Designed consumer financial touchpoints — payments, account management, and service discovery — balancing usability with regulatory requirements</li>
      <li><strong>VMware:</strong> Integrated Workday HR workflows with compliance tools for vaccination attestations and hybrid work policies</li>
    </ul>
    <br />
    <a href="/resume">→ View full resume</a>
  </div>

  <h2>Related Work</h2>
  
  <ul class="portfolio-list">
    {filteredEntries.map((entry) => (
      <li class="portfolio-list-item">
        {entry.data.heroImage && (
          <a href={`/portfolio/${entry.slug}?via=regulated`} class="portfolio-thumbnail">
            <img src={entry.data.heroImage} alt={entry.data.title} />
          </a>
        )}
        <div class="portfolio-list-content">
          <a href={`/portfolio/${entry.slug}?via=regulated`} class="portfolio-title">{entry.data.title}</a>
          <span class="portfolio-company"> — {entry.data.company}</span>
          <p>{entry.data.description}</p>
        </div>
      </li>
    ))}
  </ul>
  
  <p><a href="/portfolio">→ View entire portfolio</a></p>
</Layout>
